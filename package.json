{
  "engines": {
    "node": "14.18.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "serve": "^13.0.2"
  },
  "scripts": {
    "dev": "cd frontend && react-scripts start",
    "start": "serve -s build",
    "build": "cd frontend && npm i && react-scripts build",
    "test": "cd frontend && react-scripts test --env=jsdom",
    "eject": "cd frontend && react-scripts eject",
    "heroku-postbuild": "npm run build"
    }
}
